# Copy this file to .env and fill in real values.

PORT=5000
NODE_ENV=development

# MongoDB Atlas connection string (Atlas is a replica set, so DB transactions work).
# A plain local mongod is NOT a replica set - the code falls back automatically,
# but Atlas is strongly recommended.
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/stb_ct?retryWrites=true&w=majority

# Any long random string
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Frontend origin for CORS, and used to build verification/reset links
CLIENT_URL=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# This backend's own reachable URL, used to build absolute avatar image URLs
BACKEND_URL=http://localhost:5000

# Resend (https://resend.com) - leave RESEND_API_KEY unset for local dev;
# verification/reset links are logged to the console instead of emailed.
RESEND_API_KEY=
RESEND_FROM_EMAIL="STB-CT <onboarding@resend.dev>"
CONTACT_RECEIVER_EMAIL=teamcarbonclock@example.com

# Shared secret the Python recommendation engine sends as X-Engine-Secret
# when calling POST /api/v1/recommendations/preview. Any long random string;
# must match ENGINE_SHARED_SECRET in engine/.env.example.
ENGINE_SHARED_SECRET=change_this_to_a_long_random_string

# Where the Python recommendation engine (engine_server.py) is running.
RECOMMENDATION_ENGINE_URL=http://localhost:8421
