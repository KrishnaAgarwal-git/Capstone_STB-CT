# Copy to .env and load it yourself (e.g. `export $(cat .env | xargs)` before
# running), or just set these as real environment variables. engine_server.py
# reads them via os.environ with sane localhost defaults if unset.

# Where the Node backend (the real, scientific carbon calculator) is running.
NODE_BACKEND_URL=http://localhost:5000

# Must match ENGINE_SHARED_SECRET in Backend/.env exactly - this is the
# shared secret sent as the X-Engine-Secret header when this service calls
# POST {NODE_BACKEND_URL}/api/v1/recommendations/preview.
ENGINE_SHARED_SECRET=change_this_to_a_long_random_string

# Port this Python service listens on. The Node backend's
# RECOMMENDATION_ENGINE_URL should point here.
ENGINE_PORT=8421
